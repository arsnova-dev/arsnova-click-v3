---
import { GITHUB_URL } from '@/config/github';
const appUrl = Astro.url.searchParams.get('app') ?? 'https://app.arsnova.click';
---

<section class="bg-slate-900 px-4 py-16 sm:px-6 sm:py-24" aria-labelledby="cta-heading">
  <div class="mx-auto max-w-3xl text-center">
    <h2 id="cta-heading" class="font-display text-3xl font-bold tracking-tight text-white sm:text-4xl">Bereit für die erste Session?</h2>
    <p class="mt-4 text-lg text-slate-300">Kein Account, keine Installation. Ein Klick – Sie leiten Ihre Teilnehmer:innen per Code oder QR-Code.</p>
    <a href={appUrl} class="mt-8 inline-flex items-center rounded-xl bg-brand-500 px-8 py-4 text-lg font-semibold text-white shadow-lg shadow-brand-500/30 transition hover:bg-brand-400 focus:outline-none focus:ring-2 focus:ring-brand-400 focus:ring-offset-2 focus:ring-offset-slate-900">
      Jetzt starten
      <span class="ml-2" aria-hidden="true">→</span>
    </a>
    <p class="mt-6">
      <a href={GITHUB_URL} class="text-sm text-slate-400 hover:text-white" target="_blank" rel="noopener noreferrer">Open Source auf GitHub</a>
    </p>
  </div>
</section>
